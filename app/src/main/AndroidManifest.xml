<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Phrases" android:label="@string/Phrases">
        </activity>
        <activity android:name=".Colors" android:label="@string/Colors" >

        </activity>
        <activity android:name=".Family" android:label="@string/Family" >

        </activity>
        <activity android:name=".Numbers" android:label="@string/Numbers" >

        </activity>
        <activity android:name=".SplashActivity" android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>